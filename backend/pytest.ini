[pytest]
minversion = 7.0
pythonpath = .
testpaths = tests
asyncio_mode = auto
; No coverage option here: scope, threshold and precision are supplied by the
; runner command. Rows D195, D226, D252, D291 and D361 of
; docs/testing/DECISION-LOG.md; asserted by backend/tests/test_coverage_gate.py.
addopts = -ra --strict-markers --junitxml=reports/junit.xml
junit_family = xunit2
; Captured logs travel with a failing <testcase> only; `correlation_id` is set on
; every record by tests/conftest.py. Rows D361 and section 8 of
; backend/tests/README.md.
junit_logging = log
junit_log_passing_tests = false
log_cli = true
log_cli_level = INFO
log_cli_format = %(asctime)s %(levelname)-8s [%(correlation_id)s] %(name)s: %(message)s
log_cli_date_format = %Y-%m-%d %H:%M:%S
log_format = %(asctime)s %(levelname)-8s [%(correlation_id)s] %(name)s: %(message)s
log_date_format = %Y-%m-%d %H:%M:%S
filterwarnings =
    ignore:The 'app' shortcut is now deprecated:DeprecationWarning
    ignore:You are using a (non-supported )?Python version:FutureWarning:google\.api_core\._python_version_support
markers =
    unit: isolated unit test; every external client and boundary is patched.
    integration: exercises the FastAPI HTTP surface through starlette TestClient.
