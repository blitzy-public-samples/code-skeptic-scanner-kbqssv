# test stack
pytest==8.4.2
pytest-asyncio==0.26.0
pytest-cov==6.1.1
coverage==7.10.7
httpx==0.27.2
freezegun==1.5.5
pytest-xdist==3.8.0

# runtime stack the tests exercise
fastapi==0.95.2
starlette==0.27.0
pydantic==1.10.13
uvicorn==0.22.0
tweepy==3.10.0
openai==0.27.8
google-cloud-firestore==2.11.1
google-cloud-bigquery==3.11.4
google-auth==2.22.0
google-api-core==2.11.1
# SECURITY EXCEPTION, ESCALATED - pinned at the value AAP 0.6.1 declares. 3.3.0
# is affected by CVE-2024-33663 / PYSEC-2024-232 (algorithm and key confusion)
# and CVE-2024-33664 / PYSEC-2024-233 (compressed-JWE decompression bomb), both
# fixed in 3.4.0. Neither is reachable from this suite: app/core/security.py
# only encodes and decodes HS256 with an explicit key, so no JWE is decrypted
# and no OpenSSH ECDSA key is loaded. Raising the pin is a change to a frozen
# specification and belongs to whoever owns that specification, so it is
# escalated as an open item rather than made here.
# See docs/testing/DECISION-LOG.md.
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
pandas==1.5.3
numpy==1.24.4
sqlalchemy==1.4.49
