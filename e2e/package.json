{
  "name": "code-skeptic-scanner-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright end-to-end layer for Code Skeptic Scanner, driven by its own self-contained Vite harness. Every script names a local binary; none is resolved through npx, which would fall back to the registry when the local one is absent.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "report": "playwright show-report",
    "harness": "node ./node_modules/vite/bin/vite.js --config vite.harness.config.ts",
    "browsers:verify": "playwright install --dry-run chromium",
    "install:browsers": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "1.44.1",
    "vite": "4.5.14",
    "@vitejs/plugin-react": "4.0.4"
  }
}
